- content_for :banner_bg do
  .poster{style: "background:url(#{image_path('bk-161.jpg')}) no-repeat center; background-size: cover"}
- content_for :heading do
  COLLECTION
- content_for :grid_items do
  - @products.each do |p|
    %li.grid-item.products
      = render partial: 'shared/tile', locals: { resource: p }
