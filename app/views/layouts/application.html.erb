<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="ROBOTS" content="NOODP" />
  <meta name="viewport" content="initial-scale=1" />
  <script type="text/javascript" src="https://js.stripe.com/v2/"></script>
  <title><%= title %></title>
  <%= stylesheet_link_tag :application, media: "all" %>
  <%= csrf_meta_tags %>

</head>
<body class="<%= body_class %>">
  <!-- <%= render "navigation" %> -->
  <%= debug params %>
  <%= "Current customer - #{current_customer.email}" %> <br>
  <%= "Latest customer CartProducts#create - #{Customer.find(session[:customer_id]).email}" if session[:customer_id]%>
  <%= render "flashes" -%>
  <%= yield %>
  <%= render "javascript" %>
</body>
</html>
