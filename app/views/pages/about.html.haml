- content_for :banner_bg do
  .poster{style: "background:url(#{image_path('bk-105.jpg')}) no-repeat center; background-size: cover"}
- content_for :image do
  = image_tag('bk_1.jpg')
- content_for :heading do
  = @post.title.upcase
- content_for :body do
  = simple_format @post.body
